{
  "private": true,
  "name": "mopad-client",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc -p client && browserify client/bin/index.js -o ./client/public/index.js",
    "serve": "browserify-server --server=client/public --port 8080"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "react": "^16.1.1",
    "react-dom": "^16.1.1",
    "react-router-dom": "^4.2.2"
  },
  "devDependencies": {
    "browserify": "^14.5.0",
    "browserify-server": "^2.1.18"
  }
}
